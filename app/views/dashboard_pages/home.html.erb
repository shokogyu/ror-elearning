<%= provide(:title, "Home") %>

<% if logged_in? %>
    <div class="row">
        <div class="col-md-3 profile">
            <div><%= gravatar_for(current_user, 140) %></div>
            <div><%= current_user.name %></div>
            <div>Learned 120 words</div>    
        </div>

        <div class="col-md-9 dash-activity">
            <div class="row">
                <div class="col-md-6">
                    <%= link_to "Word", "#", class: "btn btn-warning " %>
                </div>

                <div class="col-md-6">
                    <%= link_to "Lesson", categories_path , class: "btn btn-warning" %>
                </div>
            </div>

            <div class="activity">
                <h2 class="text">Activity</h2>
                    test
            </div>
        </div>
    </div>

<% else %>
    <div class="jumbotron center">

        <h1 class="display-4">Hello, World!</h1>

        <p class="lead">
            This is a simple <span class="letter">e-leraning system</span> impletemented by Ruby on Rails :)
        </p>

        <hr class="my-4">
        <p>
        We will provide services which you can learn new language.
        To start your lessons, please sign up first :)
        </p>

        <%= link_to "Sign Up", signup_path,  class: "btn btn-primary btn-sm" %>
    </div>
<% end %>
